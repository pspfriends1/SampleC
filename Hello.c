#include <stdio.h>
int main()
{
   char Name[30];
   
   // printf() displays the string inside quotation
   printf("Hello, World!");
   printf("Enter Ur Name");
   scanf("%s",Name);
   printf("So its  %s then\n",Name);
   prinf("Some Changes Finally");
   return 0;
}
